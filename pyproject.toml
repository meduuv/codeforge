[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="codeforge"
version="0.1.0"
description="Dependency-free source inspection helpers"
requires-python=">=3.9"
license={text="MIT"}
[tool.setuptools.packages.find]
where=["src"]
